# 2026-06-08 인스턴스와 클래스

## 오늘 배운 내용

- 객체(오브젝트)란? 현실 세계의 모든 객체
- 클래스 (class): 오브젝트를 가상세계 용으로 구체화한 것
    - 인스턴스를 생성하기 위한 틀이 클래스 (붕어빵 틀)
    - 클래스에는 속성을 필드로, 동작을 메소드로 선언
    - final 이 붙은 필드는 상수 필드로서 값이 불변
    - this 는 자기 자신의 인스턴스를 표시하는 키워드
- 인스턴스 (instance): 클래스를 활용 해 메모리 상에 만들어 낸 것 (붕어빵)
    - new 키워드를 사용하여 클래스로부터 인스턴스를 생성
    - 어떤 클래스 타입 변수에 인스턴스가 담겨 있을 때 "변수명.필드명" 이나 " 변수명.메소드명()"으로 그 인스턴스의 필드나 메소드를 이용할 수 있다.
- 함수: input 과 output 갯수가 동일

## 기억할 것

- main (public static void main(String[] args)) 자동완성
- sout (System.out.printlf()) 자동완성
- 클래스명: 명사(첫 번째 글자는 대문자): (Hero)
- 필드 명: 명사(최초 이외의 단어의 맨 처음은 대문자): (itemList)
- 메소드 명: 동사(최초 이외의 단어의 맨 처음은 대문자): (itemList)

## 실습 코드

```java
public class Main {
    public static void main(String[] args) {
        // 인스턴스 생성
        // Hero hero = new Hero();
        // int a = 10;
        // long l = 10L; // literal

        Cleric cleric = new Cleric("클레릭", 30, 7);

        int mpRecovery = cleric.pray(3);
        System.out.println(mpRecovery);
    }
}


public class Cleric {
    String name;

    int hp;
    final int maxHp = 50;
    int mp;
    final int maxMp = 10;

    public Cleric(String name, int hp, int mp) {
        this.name = name;
        this.hp = hp;
        this.mp = mp;
    }

    void status() {
        System.out.println(name);
        System.out.println(hp + "/" + maxHp);
        System.out.println(mp + "/" + maxMp);
    }

    void selfAid() {
        if (mp >= 5) {
            this.mp -= 5;
            this.hp = maxHp;
        }
    }

    public int pray(int i) {

        int beforeMp = mp;

        int mpRecovery = (int) (Math.random() * 3) + i;

        mp += mpRecovery;
        if (mp >= maxMp) {
            mp = maxMp;
        }

        return mp - beforeMp;
    }
}

```

## 어려웠던 점

- pray에서 회복 결과가 아닌, 회복량을 구하는 법에 대해 고민을 많이함.
- test에서 랜덤한 회복량을 검증해야 하는 지를 모름.

## 해결 방법

- 회복하기 전의 mp값을 변수에 넣고, 회복된 mp값 - 회복하기 전의 mp값으로 값을 리턴했다.
- ChatGPT를 통해 JUnit(assertTrue)을 사용해서 범위 지정하는 법을 알게 됨.

## 내일 더 공부할 것

- JUnit 테스트 작성법
- 클래스의 책임 나누기