# 2026-06-09 클래스와 참조 및 과제

## 오늘 배운 내용

- Pull Requests 를 하게 되면
    - coderabbitai 이라는 Bot 이 자동으로 코드 검사를 하며 추가하면 좋을 것 수정해야할 것을 알려준다.

- Refactor 기능
    - class 이름을 잘못 썼다면 이 기능을 써서 프로젝트 내 class 이름을 전체 바꿔준다.
    - 'Shift+6' 단축키를 외우는게 좋다.

- 매직넘버는 좋지 않기 때문에 상수로 정의하여 의미 있는 이름을 붙여서 하는게 좋다.
    - final int Second = 5;

- 주소안에 값이 변하면 주소를 가리키는 값도 같이 변한다.
    - new Hero();
    - 참조형 즉 Heap 영역에 있는 것.

- 선언과 생성은 다르게 구분을 지어서 한다.
    - int hp;
    - Hero() {this.hp = 10;}

- 기본 type 들은 값을 가지고 있다.
    - int, short, long, char, boolean

- int[], String 들은 값을 가지고 있지 않아 null 값이다.

- 생성자의 오버로드는 2개 이상의 생성자를 만들 수 있다.
    - 기본 생성자를 넣을 수 있다.

    1. Hero(){};
    2. Hero(String name){}

- static을 하게 되면 변수에 . 을 찍는 것이 아니라 class 이름에다가 . 을 찍는다.
    - hero.money 가 아닌 Hero.money 로 해야한다.
    - statice 은 모두가 공유하는 자원일 때 쓰인다.
    - class 안에 static 이 있다면, static 문맥에서는 this를 쓸 수 없다.
        - 만약 사용해야한다면 static에 Hero hero = new Hero(); 로 불러서 사용해야한다.
    - static은 static 끼리 사용할 수 있다.

## 기억할 것

- 주소값 즉, 참조는 값이 변하면 주소만을 가리키고 있는 것도 변한다

```java
Hero hero1 = new Hero();
hero1.hp =100;
Hero hero2 = hero1;
hero2.hp =200;

이와 같이
하게 되면
hero1.hp 가 200
이 된다.
```

- 그 외 오늘 배운 내용을 메모 한 것을 기억해야 한다.

=========

## 실습 코드

```java
class ClericTest2 {

    @Test
    void main() {
        Cleric cleric = new Cleric("아서스", 40, 5);
        // 객체가 제대로 생성되었는지 눈으로 확인 시스템
        System.out.println(cleric.name + " " + cleric.hp + " " + cleric.mp);
        assertEquals("아서스", cleric.name, "캐릭터 이름이 다릅니다.");
        assertEquals(40, cleric.hp, "HP가 40이어야 합니다.");
        assertEquals(5, cleric.mp, "MP가 5이어야 합니다.");

        Cleric cleric2 = new Cleric("아서스", 35);
        // 객체 이름, HP, 최대 MP 생성되었는지 눈으로 확인 시스템
        System.out.println(cleric2.name + " " + cleric2.hp + " " + cleric2.mp);
        assertEquals("아서스", cleric2.name, "캐릭터 이름이 다릅니다.");
        assertEquals(35, cleric2.hp, "HP가 35이어야 합니다.");
        assertEquals(10, cleric2.mp, "MP가 10이어야 합니다.");

        Cleric cleric3 = new Cleric("아서스");
        // 객체 이름, 최대 HP, 최대 MP 생성되었는지 눈으로 확인 시스템
        System.out.println(cleric3.name + " " + cleric3.hp + " " + cleric3.mp);
        assertEquals("아서스", cleric3.name, "캐릭터 이름이 다릅니다.");
        assertEquals(50, cleric3.hp, "HP가 50이어야 합니다.");
        assertEquals(10, cleric3.mp, "MP가 10이어야 합니다.");

        // 다음과 같이 부르면 컴파일 자체가 되지 않음.
//        Cleric cleric4 = new Cleric();
    }
}
```

## 어려웠던 점

- 아직까지 assert로 Test 를 진행하는 면에서 어려운 점이 있다.
- 생성자를 만들어서 사용하지 않았어서 헷갈린 면이 발생한다.
- 변수에 . 을 찍는 것과 static은 class 이름에다 . 찍는 것에 대한 헷갈림?

## 해결 방법

- System.out.println 으로 시각적 테스트 진행
- assert 테스트는 아직까지 AI 도움을 받고 하고 있다.

## 내일 더 공부할 것

- 생성자를 만드는 것에 대해 익숙해질 수 있도록
