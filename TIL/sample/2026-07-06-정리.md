# 2026-06-30

## 오늘 배운 내용
객체(obj)란? 모든 물건, 대상, 목표
- 길이 확인 메서드
length()
isEmpty()

- 검색관련 메서드
반환형태: boolean
contains() // 후보 중에 해당 문자가 존재 여부 판단
endsWith() // 문자열이 지정한 문자열로 끝나는지(마지막 단어)확인

반환형태: int
indexOf()
lastIndexOf()

- 문자열 결합 3가지 방법
1. + (문자열)연산자를 이용
2. StringBuilder (실무에서 사용)
문자열 탐색 [모두 true/false로 판단]
i번째 글자가 모음인지 알려주는 메서드 : isVowel() 📌 //공백없이 작성해야함, 공백 있을 경우 문자로 처리되어 위험
i번째 글자가 자음인지 알려주는 메서드 : isConsonant() 📌
3. StringBuffer 는 빌더와 같지만 동기화를 지원한다고 한다.(코드 꼬임 방지한다고 한다는데 자세한 설명은 없음..)
코드의 예)
3. 1. 
StringBuilder sb = new StringBuilder(); 
sb.append("나혼자");

StringBuffer sbf = new StringBuffer();
sbf.append("네트워크데이터");



split() : 정규식 패턴을 기준으로 문자열 분리
replaceAll() : 패턴에 맞는 부분을 전부 교체

- 대소문자 변환하여 저장하거나 출력하는 메서드
toUpper 대문자
tolowercase 소문자

문자열 배열의 비교 :  equals로 📌
필드(변수)로 선언했던 스펠링을 equalsignoreCase()메서드로 비교

[ 단축키 ]

- Ctrl+Alt :: 함수가 작성된 실제 위치로 이동
- Ctrl+B :: 함수 자세히 보기
- Ctrl+H :: 상속관계 보기
- shift + F6 :: 이미 정해진 클래스 함수의 이름을 전체 변경하려 할때
- Alt+Enter :: 자동생성
- ctrl+shift+T :: TEST 파일 생성

## 실습 코드

```java
public class Cleric {


}
