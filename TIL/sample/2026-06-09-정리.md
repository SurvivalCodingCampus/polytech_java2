# 2026-06-08 연

## 오늘 배운 내용

- reference || primitive (값이 볷사)
- Ctrl+Alt 코드 안 확인
- 생성자 오버로드(2개 이상의 생성자)
- (static 정적/클래스 멤머(복사가능 공용자원에 위치) || instance 객체멤버로 복사)
- unitest(간단 테스트) whithe 내부로직 제어흐름 데이터 흐름 검증, blacboxtest (주로하는것)인풋 아웃풋 기반하여 설계
- 하나의 생성자(Cleric함수 하나 선언 > 그 아래 일반 변수+ static변수 선언 > void함수처럼 여러개 선언했지만 새로운 함수가 아닌 원래 함수에서 변수들을 불러와 여러 생성자들을 만드는 과정 )

## 기억할 것

- 일반변수+ 전역변수 static 선언
- class+해당클래스 TEST명 {Cleric2 clericA = new Cleric2}로 작성

## 실습 코드

```java
public class Cleric {


}
```

## 어려웠던 점

- 이전에는

## 해결 방법

- 결과를 돌려줄 필요가 없으면 `void`를 사용한다고 정리했다.
- 함수 안으로 들어오는 값은 매개변수, 함수 밖으로 나가는 값은 반환값이라고 생각했다.
- `return` 아래에 코드를 작성하면 실행되지 않을 수 있다는 점을 예제로 확인했다.

## 내일 더 공부할 것

- 함수 이름을 더 잘 짓는 방법
- 매개변수가 여러 개인 함수 작성하기
- 조건문과 반복문을 함수로 분리하는 연습
