# 2026-06-15

## 오늘 배운 내용

- 캡슐화
  class {필드 위치}, void {메소드 위치}
  class private
  ▶ private 필드 접근 법 set(저장/수정)+name{}, get(읽)+name{}함수로 접근
  main 클래스에서는 꼭 set > get 순서대로 작성해야됨
  ▶get먼저 쓸 경우 null이 나옴
- 컬랙션  
  ▶ Set
  ▶ List=index VS Map(key, value)
  ▷ list(무조건 번호넣어 꺼내기)
  ▶ ArrayList(일반 배열과 다르게 칸 수를 정하지 않아) || 자동 크기 증가
  ▷ ArrayList<Integer, Double, Boolean, Character>로 <>안에 넣어 타입을 참조할 수 있도록 한다.
  ▷ add()로 데이터 추가
  ▷ 🥲 get(index)로 꺼냄 || class에서 final 변수 일때에도 get+변수(){}이런 형식으로 작성

- setter 내부에서 타당성 검사
  throw new IllegalArgumentException(“메세지");” 를 작성하여 프로그램을 중단

## 기억할 것

- set > 하고 나중에 필요하면 get함수 추가해서 읽기
- throw new IllegalArgumentException(“메세지"); || break;와 같은 역할
- this.해당 변수=해당변수 로 저장시키도록
- 작성했던 코드를 TEST파일 만들어서 한다면
- final 필드 상수
- static 공용 변수
- this.+(자기자신 변수) ::자기자신 복제
- 클래스의 함수(메소드)를 ▶▶▶ Constructor(생성자)할 경우 ▶▶▶ TEST 파일에서 new로 복제
- ⭐System.out.println(무조건 실행하게 만들 수 있어 오류를 보기 힘듬)보다
- ⭐assertEquals를 사용해 테스트 코드(JUnit)에서 "내가 프로그래밍한 결과가 진짜 정답과 일치하는지 컴퓨터에게 채점을 맡기는 함수
- TEST파일 만드는 법 :: class 아래 void 함수의 변수명을 클릭 ▶▶▶ Generate 클릭 ▶▶▶ Test
  [TEST파일 만드는 순서]
  package com.survivalcoding;
  import org.junit.jupiter.api.Test;
  import static org.junit.jupiter.api.Assertions.assertEquals;
  class 변수
  {
  void 변수
  }
- [단축키]
- Ctrl+Alt :: 함수가 작성된 실제 위치로 이동
- shift + F6 :: 이미 정해진 클래스 함수의 이름을 전체 변경하려 할때

## 실습 코드

```java
public class Cleric {


}
```

## 어려웠던 점

-

## 해결 방법

-

## 내일 더 공부할 것

- 